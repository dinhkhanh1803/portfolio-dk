"use client";
import Link from "next/link";
import { ArrowRight, Code2, MapPin, Sparkles } from "lucide-react";
import { ItemCards, ProjectCards } from "./content-cards";
import { useLanguage } from "./language-provider";
import { portfolio } from "./portfolio-data";
export default function Home() { const { language } = useLanguage(); const content = portfolio[language]; const selectedProjects = content.projects.slice(0, 3); const vi = language === "vi"; return <main><section className="home-hero"><div><p className="eyebrow"><Sparkles size={15} /> Full-stack Developer</p><h1>{content.profile.name}</h1><p className="hero-summary">{content.profile.summary}</p><p className="location"><MapPin size={16} /> {content.profile.location}</p><div className="hero-actions"><Link className="button button-primary" href="/projects">{vi ? "Xem dự án" : "View projects"}<ArrowRight size={18} /></Link><Link className="button button-secondary" href="/contact">{vi ? "Liên hệ" : "Get in touch"}</Link></div></div><div className="hero-art"><Code2 size={80} /><span>01</span><strong>Build · Learn · Share</strong></div></section><section className="page-section"><p className="eyebrow">{vi ? "Dự án nổi bật" : "Selected work"}</p><h2>{vi ? "Sản phẩm là bằng chứng tốt nhất." : "Products are the clearest proof."}</h2><ProjectCards items={selectedProjects} /></section><section className="page-section"><p className="eyebrow">{vi ? "Năng lực" : "Capabilities"}</p><h2>{vi ? "Kỹ thuật, sản phẩm và sự tò mò." : "Engineering, product and curiosity."}</h2><div className="skill-strip">{content.profile.skills.map((skill) => <article key={skill.title}><h3>{skill.title}</h3><p>{skill.items.join(" · ")}</p></article>)}</div></section><section className="page-section"><p className="eyebrow">{vi ? "Đang xây dựng" : "What I am building"}</p><h2>{vi ? "Không chỉ là portfolio." : "More than a portfolio."}</h2><ItemCards items={[content.tools[0], content.playground[0], content.posts[0]]} /></section></main>; }
